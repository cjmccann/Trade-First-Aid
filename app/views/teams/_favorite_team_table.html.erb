<div style="width:90%; margin: auto;">
<table id="teamTable" class="table table-striped table-condensed" data-toggle="table" data-sort-name="name">
  <thead>
    <tr>
      <th data-field="photo"></th>
      <th data-field="name" data-sortable="true">Name</th>
      <th data-field="pos" data-sortable="true">Pos</th>

      <% stat_order_arr = [] %>
      <% @team.league.stat_settings.each do |key, val| %>
        <% next if val[:roto_names].nil? %>
        <th data-field="<%= key %>" data-sortable="true"><%= key %></th>
        <% stat_order_arr.push(key) %>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @team.rotoplayer_arr.each_with_index do |roto_id, i| %>
      <tr id="tr-id-<%= i %>" class="tr-class-<%= i % 2 %>">
        <td><img class='playerPhoto' src='<%= @team.player_metadata[roto_id]['photo'] %>'></td>
        <td id="td-id-<%= i %>" class="td-class-<%= i %>"><%= @team.player_metadata[roto_id]['name'] %></td>
        <td data-field="pos"><%= @team.player_metadata[roto_id]['position'] %></td>

        <% stat_order_arr.each do |stat_id| %>
          <td><%= @team.league.player_stats[roto_id][stat_id] || 0 %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
