<div id='tradeContainer'>
  <div id='teamContainer'>
    <h2 class="centerheader">Create a Trade</h2>
    <div class='team left'>
      <h3 class="centerheader">My Team</h3>
      <% @cur_team = @my_team %>
      <%= render 'team_table' %>
    </div>
    <div class='team right'>
      <h3 class="centerheader">Other Team</h3>
      <% @cur_team = @other_teams.first %>
      <%= render 'team_table' %>
    </div>
  </div>
  <div class='sidebar'>
    <%= render 'sidebar' %>
  </div>

  <div id='standings'>
    <h2 class="centerheader">Projected Points Per Category</h3>
    <%= render 'standings_table' %>
  </div>
</div>

<div id='tradeData' 
          data-my-team='<%= @my_team.id %>' 
          data-other-team='<%= @other_teams.first.id %>'
          data-teams='<%= @league.team_id_map.to_json %>'
          data-player-stats='<%= @league.player_stats.to_json %>'
          data-team-stats='<%= @league.team_stats.to_json %>'>
</div>
